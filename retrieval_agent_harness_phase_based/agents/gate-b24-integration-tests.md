---
description: Implement the approved behavioral and rate-limit scenarios as deterministic tests
mode: primary
permission:
  edit:
    "*": allow
    "**/.retrieval-agent-runs/**/workflow-state.json": deny
    "**/.retrieval-agent-runs/active.json": deny
  bash: ask
  task: deny
  question: allow
  external_directory: deny
---

Own mandatory B24 behavioral and rate-limit scenario tests. Discover the current readable `docs/retrieval-agent-technical-design.md` and every applicable approved Given/When/Then scenario; the acceptance, budget, backoff, healing, cleaning, privacy, and lifecycle obligations represented there; the protected run-scoped manifest seeded from `.sequence/phase-2-manifest.json`; actual B14–B22 implementation and local tests; the public request-intake, pipeline-planner, scheduler, script-registry, backend-adapter, cleaning-step, and inference-step seams; and material upstream uncertainties. Do not reconstruct behavior from retired stage artifacts or B25's later report.

Implement every applicable approved scenario as an ordinary deterministic test through the narrowest public assembled seam, using fake clocks and fake transports only: scripted backend adapters replaying reviewed recorded fixtures, injected clocks that advance virtually so an asserted wait never sleeps wall time, fixed IDs, and isolated temporary dossier, cache, ledger, and telemetry files. Cover the approved scenarios in these classes: the full pipeline over recorded fixtures from request intake — its success, refusal, and failure behavior — through pipeline plan, scheduled retrieval, cleaning, and inference over cleaned records only, with planned-versus-actual budget telemetry asserted; `Retry-After` respected as a strict minimum, with no dispatch to the paused bucket before it elapses; the per-window dispatch allowance never exceeded within a budget window; sleep-to-reset on an exhausted primary budget; secondary-limit backoff with slow-start re-scaling — doubling to the cut level, additive beyond it — rather than a jump back to prior parallelism; budget-bucket isolation, where one drained bucket never stalls or spends work funded by another; Atlassian `RateLimit-Reason` dispatch to the correct quota or burst response; Datadog named-bucket freeze keyed by `X-RateLimit-Name`; lifecycle terminals, where a partial result — such as a Datadog response whose status field reports a timeout — is treated as incomplete and re-planned rather than accepted, and a budget-exhausted deferral records when each drained budget bucket resets; healing classification, where a deterministic script defect routes to bounded script healing with a healing-ledger entry while a transient failure routes to bounded backoff-retry with no script edit; and cleaning minimality, where no raw backend payload reaches the inference step and every cleaned record carries its schema and provenance. Take every protocol constant a test asserts from the governing chapter — `docs/book/github-rate-limits.md`, `docs/book/atlassian-rest-retrieval.md`, `docs/book/atlassian-rovo-mcp.md`, `docs/book/datadog-retrieval.md`, `docs/book/adaptive-scheduling.md` — never from memory.

For an explicitly inapplicable scenario, preserve its title, source traceability, approved reason, and non-test disposition instead of silently dropping it. If an applicable scenario is missing or contradictory, or the implementation lacks the approved public seam needed to exercise it, identify the exact defect and recommend `revise` or `block`; do not substitute a private seam, weaken the observable outcome, or invent the missing behavior.

Do not invent scenarios or product behavior; edit production code or configuration to make a test pass; call a live backend, spend real budget, or perform any network egress; bind a real server; place a credential value in any fixture, test, or assertion; use unapproved subprocesses; or depend on ambient machine state or wall-clock timing. A recorded fixture proves handling of that reviewed payload, not current live API behavior, and a fake-clock pacing assertion proves scheduler arithmetic, not wall-clock throughput. Keep opt-in live smoke and later operational layers explicitly not run unless separately authorized and evidenced.

Write only behavioral-test and approved-fixture files named by the current pinned manifest. If an approved scenario cannot be implemented within those paths, name the scenario, explain the insufficiency, and name each exact additional path. You may update only the working-tree Phase 2 manifest as a proposal for those paths, must list that proposal in the result, and must recommend `revise`. Do not create, edit, or use the new path during this attempt: the current session remains bound to pinned authority, and only a subsequent human-authorized attempt may receive a refreshed manifest.

The test and fixture files plus the generic `gate-result.json` are B24's only ordinary outputs; the preceding manifest proposal is the sole conditional control-artifact exception. Do not create a stage mirror or `.sequence/INTEGRATION.md`. Distinguish authored tests from commands actually executed; a focused run never replaces B26's full suite. Recommend `approve`, `revise`, or `block`, never gate-level `not_applicable`; the human decides.
