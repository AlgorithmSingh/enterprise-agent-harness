# Prototype checklist

Control board for prototype-assisted design decisions. `.prototype/README.md` is the artifact index; this file is the decision/status source.

| # | Slug | Decision question | Evidence needed | Failure signal | Blocks | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 001 | rate-limit-scheduler | Is the D07 scheduler contract (per-bucket limiters, header-driven pacing, utilization ceiling, AIMD with slow-start recovery, full-jitter backoff, Retry-After precedence) implementable as one small stdlib-only Python module fully testable with a fake clock? | Passing deterministic unittest run proving ceiling, Retry-After exactness, multiplicative decrease + slow-start, bucket isolation, jitter bounds | Contract needs live timing, hidden state, or nondeterminism to work | D07 prompt final wording; B19 scheduler body obligations | complete — accepted; 22/22 deterministic tests pass. Four contract corrections promoted: per-window allowance replaces the decaying-rate ceiling (book patched), Retry-After is a strict minimum not an exact wait, recovery keeps cut/floor/ssthresh distinct, GitHub's global concurrency cap is the one cross-bucket coupling. Known unproven: header normalization adapters, window-length discovery, multi-process coordination, point-cost budgets. |
| 002 | mcp-headless-client | Can a deterministic Python script drive an MCP server with the current `mcp` SDK, and are the book's claims about import paths, session APIs, and the mcp-remote bridge true against installed packages? | Installed-version introspection transcript + an offline in-process stdio round trip (list tools, call tool, typed result) | SDK surface contradicts the book chapter; round trip requires an LLM or network | D05/D03 prompt claims about the MCP client path | complete — accepted; deterministic stdio round trip proven against `mcp` 1.29.0. Four book corrections promoted: `tools/call` failures all surface as `isError` (never exceptions) so retry policy classifies by content and arguments must be validated client-side against `inputSchema`; the mcp-remote callback port is URL-derived (39570 for authv2), not 3334; token/debug paths are version-scoped under `~/.mcp-auth/mcp-remote-<version>/`; the pin is `mcp>=1.29,<2` because v2 reuses the import path with incompatible semantics. Live-endpoint behavior (auth, quotas, 429s) deliberately untested. |

Both prototypes are design instruments. Their spike code is throwaway; production implementation is produced only through the gate workflow.
